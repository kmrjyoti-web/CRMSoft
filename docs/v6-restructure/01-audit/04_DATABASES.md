# Prisma Databases Inventory

## Schema Files

### apps-backend/api/prisma/demo/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// DemoDB — mirrors WorkingDB schema exactly (same models, separate physical DB).
// Purpose: isolate demo/trial tenant data from production WorkingDB.
// Domain files are copied from prisma/working/v1/ via scripts/sync-demo-schema.sh.
// Run sync script after any WorkingDB schema change.
//
// Tenant routing: Tenant.status === 'DEMO' → queries go to DemoDB instead of WorkingDB.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/.prisma/demo-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("DEMO_DATABASE_URL")
}

// ─── Enums ─────────────────────────────────────────────────────────────────

enum RawContactStatus {
  RAW
  VERIFIED
  REJECTED
  DUPLICATE
}

enum RawContactSource {
  MANUAL
  BULK_IMPORT
  WEB_FORM
  REFERRAL
  API
}

enum CommunicationType {
  PHONE
  EMAIL
  MOBILE
```

Models: 0
0

### apps-backend/api/prisma/demo/v1/accounts.prisma

```prisma
// WorkingDB — accounts domain
// Generated by scripts/split-schemas.py
// Models: Invoice, InvoiceLineItem, ProformaInvoice, ProformaLineItem, CreditNote, SavedFormula, PurchaseInvoice, PurchaseInvoiceItem, LedgerMaster, LedgerMapping, AccountTransaction, BankAccount, BankReconciliation, DebitNote, DebitNoteItem, AccountGroup

model Invoice {
  id        String        @id @default(uuid())
  tenantId  String        @default("") @map("tenant_id")
  invoiceNo String        @map("invoice_no")
  status    InvoiceStatus @default(DRAFT)

  // ─── SOURCE ───
  quotationId       String?    @map("quotation_id")
  quotation         Quotation? @relation(fields: [quotationId], references: [id])
  leadId            String?    @map("lead_id")
  lead              Lead?      @relation(fields: [leadId], references: [id])
  saleOrderId       String?    @map("sale_order_id")
  deliveryChallanId String?    @map("delivery_challan_id")
  inventoryEffect   Boolean    @default(true) @map("inventory_effect")
  accountsEffect    Boolean    @default(true) @map("accounts_effect")

  // ─── CUSTOMER ───
  contactId      String?       @map("contact_id")
  contact        Contact?      @relation(fields: [contactId], references: [id])
  organizationId String?       @map("organization_id")
  organization   Organization? @relation(fields: [organizationId], references: [id])

  // ─── BILLING INFO ───
  billingName      String  @map("billing_name")
  billingAddress   String? @map("billing_address")
  billingCity      String? @map("billing_city")
  billingState     String? @map("billing_state")
  billingPincode   String? @map("billing_pincode")
  billingGstNumber String? @map("billing_gst_number")
  shippingName     String? @map("shipping_name")
  shippingAddress  String? @map("shipping_address")
  shippingCity     String? @map("shipping_city")
  shippingState    String? @map("shipping_state")
  shippingPincode  String? @map("shipping_pincode")

  // ─── SELLER INFO (snapshot) ───
```

Models: 16

### apps-backend/api/prisma/demo/v1/audit.prisma

```prisma
// WorkingDB — audit domain
// Generated by scripts/split-schemas.py
// Models: SyncChangeLog, SyncAuditLog, CronJobRunLog, ApiRequestLog, UnmaskAuditLog, AiUsageLog, ControlRoomAuditLog

model SyncChangeLog {
  id String @id @default(uuid())

  deviceId String @map("device_id")
  userId   String @map("user_id")

  // ─── CHANGE ───
  entityName String       @map("entity_name")
  entityId   String       @map("entity_id")
  action     ChangeAction

  changedFields  Json? @map("changed_fields")
  fullRecord     Json? @map("full_record")
  previousValues Json? @map("previous_values")

  clientTimestamp DateTime @map("client_timestamp")
  clientVersion   Int      @default(1) @map("client_version")

  // ─── SYNC STATE ───
  isPushed     Boolean   @default(false) @map("is_pushed")
  pushedAt     DateTime? @map("pushed_at")
  pushResult   String?   @map("push_result")
  conflictId   String?   @map("conflict_id")
  errorMessage String?   @map("error_message")

  tenantId  String   @default("") @map("tenant_id")
  createdAt DateTime @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([userId, deviceId, isPushed])
```

Models: 7

### apps-backend/api/prisma/demo/v1/communication.prisma

```prisma
// WorkingDB — communication domain
// Generated by scripts/split-schemas.py
// Models: EmailAccount, EmailThread, Email, EmailAttachment, EmailTemplate, EmailSignature, EmailCampaign, CampaignRecipient, EmailTrackingEvent, EmailUnsubscribe, WhatsAppBusinessAccount, WaConversation, WaMessage, WaTemplate, WaBroadcast, WaBroadcastRecipient, WaChatbotFlow, WaQuickReply, WaOptOut, EmailFooterTemplate, CommunicationLog

model EmailAccount {
  id       String @id @default(uuid())
  tenantId String @default("") @map("tenant_id")
  userId   String @map("user_id")
  // [cross-db removed] user     User   @relation("EmailAccountUser", fields: [userId], references: [id])

  provider     EmailProvider
  label        String?
  emailAddress String        @map("email_address")
  displayName  String?       @map("display_name")

  accessToken    String?   @map("access_token")
  refreshToken   String?   @map("refresh_token")
  tokenExpiresAt DateTime? @map("token_expires_at")
  oauthScope     String?   @map("oauth_scope")

  imapHost     String?  @map("imap_host")
  imapPort     Int?     @map("imap_port")
  imapSecure   Boolean? @map("imap_secure")
  smtpHost     String?  @map("smtp_host")
  smtpPort     Int?     @map("smtp_port")
  smtpSecure   Boolean? @map("smtp_secure")
  smtpUsername String?  @map("smtp_username")
  smtpPassword String?  @map("smtp_password")

  status        EmailAccountStatus @default(ACTIVE)
  lastSyncAt    DateTime?          @map("last_sync_at")
  lastSyncError String?            @map("last_sync_error")
  syncToken     String?            @map("sync_token")
  syncFromDate  DateTime?          @map("sync_from_date")
  syncEnabled   Boolean            @default(true) @map("sync_enabled")

  isDefault       Boolean @default(false) @map("is_default")
  autoLinkEnabled Boolean @default(true) @map("auto_link_enabled")
  signatureId     String? @map("signature_id")

```

Models: 21

### apps-backend/api/prisma/demo/v1/config.prisma

```prisma
// WorkingDB — config domain
// Generated by scripts/split-schemas.py
// Models: Brand, BrandOrganization, BrandContact, BusinessLocation, CompanyCountry, CompanyState, CompanyCity, CompanyPincode, CustomFieldDefinition, EntityConfigValue, SyncPolicy, SyncWarningRule, SyncDevice, SyncConflict, SyncFlushCommand, CronJobConfig, BusinessHoursSchedule, HolidayCalendar, CalendarHighlight, AutoNumberSequence, CompanyProfile, NotionConfig, ApiKey, WebhookEndpoint, WebhookDelivery, RateLimitTier, TableConfig, DataMaskingPolicy, QuietHourConfig, UserCalendarSync, UserAvailability, BlockedSlot, CalendarConfig, GoogleConnection, AiSettings, ShortcutDefinition, ShortcutUserOverride, AiModel, AiDataset, AiDocument, AiTrainingJob, AiEmbedding, AiChatSession, AiChatMessage, AiSystemPrompt, ControlRoomRule, ControlRoomValue, TenantRuleCacheVersion, ControlRoomDraft, SavedFilter

model Brand {
  id                 String              @id @default(uuid())
  tenantId           String              @default("") @map("tenant_id")
  name               String
  code               String
  description        String?
  logo               String?
  website            String?
  isActive           Boolean             @default(true) @map("is_active")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  brandOrganizations BrandOrganization[]
  brandContacts      BrandContact[]
  products           Product[]

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@unique([tenantId, name])
  @@unique([tenantId, code])
  @@index([tenantId])
  @@map("gv_cfg_brands")
}

model BrandOrganization {
  id             String       @id @default(uuid())
  tenantId       String       @default("") @map("tenant_id")
  brandId        String       @map("brand_id")
  brand          Brand        @relation(fields: [brandId], references: [id], onDelete: Cascade)
  organizationId String       @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  isPrimary      Boolean      @default(false) @map("is_primary")
```

Models: 50

### apps-backend/api/prisma/demo/v1/crm.prisma

```prisma
// WorkingDB — crm domain
// Generated by scripts/split-schemas.py
// Models: RawContact, RawContactFilter, Contact, ContactFilter, Organization, OrganizationFilter, ContactOrganization, Communication, Lead, LeadFilter, Activity, Demo, TourPlan, EntityOwner, OwnershipLog, AssignmentRule, Manufacturer, ManufacturerOrganization, ManufacturerContact, OrganizationLocation, FollowUp, RecurringEvent, TourPlanVisit, TourPlanPhoto, CalendarEvent, SupportTicket, SupportTicketMessage, Task, TaskHistory, TaskWatcher, Comment, TaskLogicConfig, ScheduledEvent, EventParticipant, EventHistory, EntityVerificationRecord, WarrantyTemplate, WarrantyRecord, WarrantyClaim, AMCPlanTemplate, AMCContract, AMCSchedule, ServiceVisitLog, ServiceCharge

model RawContact {
  id            String           @id @default(uuid())
  tenantId      String           @default("") @map("tenant_id")
  source        RawContactSource @default(MANUAL)
  status        RawContactStatus @default(RAW)
  firstName     String           @map("first_name")
  lastName      String           @map("last_name")
  designation   String?
  department    String?
  companyName   String?          @map("company_name")
  notes         String?
  isActive      Boolean          @default(true) @map("is_active")
  isDeleted                 Boolean          @default(false) @map("is_deleted")
  entityVerificationStatus  String           @default("UNVERIFIED") @map("entity_verification_status")
  entityVerifiedVia         String?          @map("entity_verified_via")
  deletedAt     DateTime?        @map("deleted_at")
  deletedById   String?          @map("deleted_by_id")
  verifiedAt    DateTime?        @map("verified_at")
  verifiedById  String?          @map("verified_by_id")
  // [cross-db removed] verifiedBy    User?            @relation("RawContactVerifiedBy", fields: [verifiedById], references: [id])
  contactId     String?          @map("contact_id")
  contact       Contact?         @relation(fields: [contactId], references: [id])
  createdById   String           @map("created_by_id")
  // [cross-db removed] createdByUser User             @relation("RawContactCreatedBy", fields: [createdById], references: [id])
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")

  filters        RawContactFilter[]
  communications Communication[]    @relation("CommRawContact")

  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([status])
  @@index([firstName, lastName])
  @@index([tenantId])
```

Models: 44

### apps-backend/api/prisma/demo/v1/documents.prisma

```prisma
// WorkingDB — documents domain
// Generated by scripts/split-schemas.py
// Models: ImportProfile, ImportJob, ImportRow, ExportJob, Document, DocumentAttachment, DocumentFolder, CloudConnection, DocumentShareLink, DocumentActivity, DocumentTemplate, TenantTemplateCustomization

model ImportProfile {
  id           String             @id @default(uuid())
  tenantId     String             @default("") @map("tenant_id")
  name         String
  description  String?
  sourceSystem String?            @map("source_system")
  icon         String?
  color        String?
  targetEntity ImportTargetEntity @map("target_entity")

  expectedHeaders String[] @map("expected_headers")
  headerMatchMode String   @default("FLEXIBLE") @map("header_match_mode")

  fieldMapping    Json  @map("field_mapping")
  defaultValues   Json? @map("default_values")
  validationRules Json? @map("validation_rules")

  duplicateCheckFields String[]          @map("duplicate_check_fields")
  duplicateStrategy    DuplicateStrategy @default(ASK_PER_ROW) @map("duplicate_strategy")
  fuzzyMatchEnabled    Boolean           @default(false) @map("fuzzy_match_enabled")
  fuzzyMatchFields     String[]          @map("fuzzy_match_fields")
  fuzzyThreshold       Decimal           @default(0.85) @map("fuzzy_threshold") @db.Decimal(3, 2)

  usageCount     Int       @default(0) @map("usage_count")
  lastUsedAt     DateTime? @map("last_used_at")
  totalImported  Int       @default(0) @map("total_imported")
  avgSuccessRate Decimal?  @map("avg_success_rate") @db.Decimal(5, 2)

  status        ProfileStatus @default(ACTIVE)
  isDefault     Boolean       @default(false) @map("is_default")
  createdById   String        @map("created_by_id")
  createdByName String        @map("created_by_name")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")

  importJobs ImportJob[]
```

Models: 12

### apps-backend/api/prisma/demo/v1/inventory.prisma

```prisma
// WorkingDB — inventory domain
// Generated by scripts/split-schemas.py
// Models: Product, ProductPrice, CustomerPriceGroup, CustomerGroupMapping, ProductTaxDetail, ProductUnitConversion, ProductRelation, ProductFilter, InventoryItem, StockLocation, StockTransaction, StockSummary, StockAdjustment, SerialMaster, BOMFormula, BOMFormulaItem, BOMProduction, ScrapRecord, InventoryLabel, UnitMaster, UnitConversion, PurchaseRFQ, PurchaseRFQItem, PurchaseQuotation, PurchaseQuotationItem, QuotationComparison, PurchaseOrder, PurchaseOrderItem, GoodsReceipt, GoodsReceiptItem, PurchaseMaster

model Product {
  id               String  @id @default(uuid())
  tenantId         String  @default("") @map("tenant_id")
  name             String
  code             String
  slug             String
  shortDescription String? @map("short_description")
  description      String?

  // Hierarchy
  parentId String?   @map("parent_id")
  parent   Product?  @relation("ProductHierarchy", fields: [parentId], references: [id])
  children Product[] @relation("ProductHierarchy")
  isMaster Boolean   @default(false) @map("is_master")

  // Media
  image       String?
  images      Json?
  brochureUrl String? @map("brochure_url")
  videoUrl    String? @map("video_url")

  // Pricing (base)
  mrp           Decimal? @db.Decimal(12, 2)
  salePrice     Decimal? @map("sale_price") @db.Decimal(12, 2)
  purchasePrice Decimal? @map("purchase_price") @db.Decimal(12, 2)
  costPrice     Decimal? @map("cost_price") @db.Decimal(12, 2)

  // Tax (Indian GST)
  taxType      TaxType  @default(GST) @map("tax_type")
  hsnCode      String?  @map("hsn_code")
  gstRate      Decimal? @map("gst_rate") @db.Decimal(5, 2)
  cessRate     Decimal? @map("cess_rate") @db.Decimal(5, 2)
  taxInclusive Boolean  @default(false) @map("tax_inclusive")

  // Unit & Packing
  primaryUnit      UnitType  @default(PIECE) @map("primary_unit")
```

Models: 31

### apps-backend/api/prisma/demo/v1/notifications.prisma

```prisma
// WorkingDB — notifications domain
// Generated by scripts/split-schemas.py
// Models: Reminder, Notification, NotificationPreference, NotificationTemplate, PushSubscription, NotificationConfig, EscalationRule

model Reminder {
  id          String          @id @default(uuid())
  tenantId    String          @default("") @map("tenant_id")
  entityType  String          @map("entity_type")
  entityId    String          @map("entity_id")
  channel     ReminderChannel @default(IN_APP)
  scheduledAt DateTime        @map("scheduled_at")
  sentAt      DateTime?       @map("sent_at")
  isSent      Boolean         @default(false) @map("is_sent")
  title       String
  message     String?         @db.Text
  description String?         @db.Text

  // ─── Enhanced fields ───
  status           ReminderStatus @default(PENDING)
  type             ReminderType   @default(PERSONAL)
  recurrenceConfig Json?          @map("recurrence_config")
  snoozedUntil     DateTime?      @map("snoozed_until")
  snoozeCount      Int            @default(0) @map("snooze_count")
  maxSnooze        Int            @default(3) @map("max_snooze")
  triggeredAt      DateTime?      @map("triggered_at")
  acknowledgedAt   DateTime?      @map("acknowledged_at")
  missedAt         DateTime?      @map("missed_at")
  taskId           String?        @map("task_id")
  task             Task?          @relation("TaskReminders", fields: [taskId], references: [id])

  // ─── NOTIFICATION PREFERENCES ───
  notifyVia Json @default("[\"PUSH\",\"IN_APP\"]") @map("notify_via")

  // ─── End enhanced ───
  recipientId String   @map("recipient_id")
  // [cross-db removed] recipient   User     @relation("ReminderRecipient", fields: [recipientId], references: [id])
  createdById String   @map("created_by_id")
  // [cross-db removed] createdBy   User     @relation("ReminderCreatedBy", fields: [createdById], references: [id])
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")
```

Models: 7

### apps-backend/api/prisma/demo/v1/payments.prisma

```prisma
// WorkingDB — payments domain
// Generated by scripts/split-schemas.py
// Models: Payment, PaymentReceipt, Refund, PaymentReminder, PaymentRecord

model Payment {
  id        String  @id @default(uuid())
  tenantId  String  @default("") @map("tenant_id")
  paymentNo String  @map("payment_no")
  invoiceId String  @map("invoice_id")
  invoice   Invoice @relation(fields: [invoiceId], references: [id])

  // ─── PAYMENT INFO ───
  amount   Decimal        @db.Decimal(14, 2)
  currency String         @default("INR")
  status   PaymentStatus  @default(PENDING)
  method   PaymentMethod
  gateway  PaymentGateway @default(MANUAL)

  // ─── GATEWAY DETAILS ───
  gatewayOrderId   String? @map("gateway_order_id")
  gatewayPaymentId String? @map("gateway_payment_id")
  gatewaySignature String? @map("gateway_signature")
  gatewayResponse  Json?   @map("gateway_response")

  // ─── OFFLINE DETAILS ───
  chequeNumber     String?   @map("cheque_number")
  chequeDate       DateTime? @map("cheque_date")
  chequeBankName   String?   @map("cheque_bank_name")
  transactionRef   String?   @map("transaction_ref")
  upiTransactionId String?   @map("upi_transaction_id")

  // ─── META ───
  paidAt        DateTime? @map("paid_at")
  notes         String?
  failureReason String?   @map("failure_reason")

  // ─── AUDIT ───
  recordedById String   @map("recorded_by_id")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")
```

Models: 5

### apps-backend/api/prisma/demo/v1/reports.prisma

```prisma
// WorkingDB — reports domain
// Generated by scripts/split-schemas.py
// Models: ReportExportLog, ReportDefinition, ReportBookmark, ScheduledReport, ReportTemplate

model ReportExportLog {
  id                String       @id @default(uuid())
  tenantId          String       @default("") @map("tenant_id")
  reportType        ReportType   @map("report_type")
  format            ReportFormat
  filters           Json?
  recordCount       Int          @map("record_count")
  fileUrl           String?      @map("file_url")
  fileSize          Int?         @map("file_size")
  status            String       @default("COMPLETED")
  errorMessage      String?      @map("error_message")
  generatedAt       DateTime     @map("generated_at")
  duration          Int?
  exportedById      String       @map("exported_by_id")
  exportedByName    String       @map("exported_by_name")
  reportCode        String?      @map("report_code")
  reportName        String?      @map("report_name")
  exportSource      String?      @default("MANUAL") @map("export_source")
  scheduledReportId String?      @map("scheduled_report_id")
  generationTimeMs  Int?         @map("generation_time_ms")
  createdAt         DateTime     @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([exportedById, createdAt])
  @@index([reportCode, createdAt])
  @@index([tenantId])
  @@map("gv_rpt_export_logs")
}

model ReportDefinition {
```

Models: 5

### apps-backend/api/prisma/demo/v1/sales.prisma

```prisma
// WorkingDB — sales domain
// Generated by scripts/split-schemas.py
// Models: Quotation, QuotationLineItem, QuotationSendLog, QuotationNegotiationLog, QuotationActivity, QuotationTemplate, SalesTarget, ServiceRate, SaleOrder, SaleOrderItem, DeliveryChallan, DeliveryChallanItem, SaleReturn, SaleReturnItem, SaleMaster, PriceList, PriceListItem

model Quotation {
  id          String          @id @default(uuid())
  tenantId    String          @default("") @map("tenant_id")
  quotationNo String          @map("quotation_no")
  status      QuotationStatus @default(DRAFT)
  version     Int             @default(1)

  // ─── BUSINESS INFO ───
  title     String?
  summary   String?
  coverNote String? @map("cover_note")

  // ─── PRICING ───
  subtotal       Decimal @default(0) @db.Decimal(14, 2)
  discountType   String? @map("discount_type")
  discountValue  Decimal @default(0) @map("discount_value") @db.Decimal(14, 2)
  discountAmount Decimal @default(0) @map("discount_amount") @db.Decimal(14, 2)
  taxableAmount  Decimal @default(0) @map("taxable_amount") @db.Decimal(14, 2)
  cgstAmount     Decimal @default(0) @map("cgst_amount") @db.Decimal(14, 2)
  sgstAmount     Decimal @default(0) @map("sgst_amount") @db.Decimal(14, 2)
  igstAmount     Decimal @default(0) @map("igst_amount") @db.Decimal(14, 2)
  cessAmount     Decimal @default(0) @map("cess_amount") @db.Decimal(14, 2)
  totalTax       Decimal @default(0) @map("total_tax") @db.Decimal(14, 2)
  roundOff       Decimal @default(0) @map("round_off") @db.Decimal(14, 2)
  totalAmount    Decimal @default(0) @map("total_amount") @db.Decimal(14, 2)

  // ─── PRICE FLEXIBILITY ───
  priceType        QuotationPriceType @default(FIXED) @map("price_type")
  minAmount        Decimal?           @map("min_amount") @db.Decimal(14, 2)
  maxAmount        Decimal?           @map("max_amount") @db.Decimal(14, 2)
  plusMinusPercent Decimal?           @map("plus_minus_percent") @db.Decimal(5, 2)

  // ─── VALIDITY & TERMS ───
  validFrom       DateTime? @map("valid_from")
  validUntil      DateTime? @map("valid_until")
  paymentTerms    String?   @map("payment_terms")
```

Models: 17

### apps-backend/api/prisma/demo/v1/tax.prisma

```prisma
// WorkingDB — tax domain
// Generated by scripts/split-schemas.py
// Models: GstVerificationLog, GSTReturn, TDSRecord

model GstVerificationLog {
  id                 String   @id @default(uuid())
  tenantId           String   @map("tenant_id")
  userId             String   @map("user_id")
  gstNumber          String   @map("gst_number")
  verificationMethod String   @map("verification_method")
  apiResponse        Json?    @map("api_response")
  isValid            Boolean  @map("is_valid")
  businessName       String?  @map("business_name")
  businessAddress    String?  @map("business_address")
  gstStatus          String?  @map("gst_status")
  approvedBy         String?  @map("approved_by")
  approvalNotes      String?  @map("approval_notes")
  createdAt          DateTime @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([tenantId, userId])
  @@index([gstNumber])
  @@map("gv_tax_gst_verification_logs")
}

model GSTReturn {
  id                String    @id @default(uuid())
  tenantId          String    @map("tenant_id")
  returnType        String    @map("return_type")
  period            String
  financialYear     String    @map("financial_year")
  b2bInvoices       Json?     @map("b2b_invoices")
  b2cLarge          Json?     @map("b2c_large")
  b2cSmall          Json?     @map("b2c_small")
```

Models: 3

### apps-backend/api/prisma/demo/v1/workflow.prisma

```prisma
// WorkingDB — workflow domain
// Generated by scripts/split-schemas.py
// Models: ApprovalRequest, ApprovalRule, Workflow, WorkflowState, WorkflowTransition, WorkflowInstance, WorkflowHistory, WorkflowApproval, WorkflowActionLog, WorkflowSlaEscalation

model ApprovalRequest {
  id          String    @id @default(uuid())
  tenantId    String    @default("") @map("tenant_id")
  entityType  String    @map("entity_type")
  entityId    String?   @map("entity_id")
  action      String
  payload     Json
  makerId     String    @map("maker_id")
  // [cross-db removed] maker       User      @relation("ApprovalMaker", fields: [makerId], references: [id])
  checkerId   String?   @map("checker_id")
  // [cross-db removed] checker     User?     @relation("ApprovalChecker", fields: [checkerId], references: [id])
  checkerRole String    @map("checker_role")
  status      String    @default("PENDING")
  makerNote   String?   @map("maker_note")
  checkerNote String?   @map("checker_note")
  expiresAt   DateTime  @map("expires_at")
  decidedAt   DateTime? @map("decided_at")
  createdAt   DateTime  @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([status])
  @@index([makerId])
  @@index([checkerRole, status])
  @@index([tenantId])
  @@map("gv_wf_approval_requests")
}

model ApprovalRule {
  id              String   @id @default(uuid())
  tenantId        String   @default("") @map("tenant_id")
```

Models: 10

### apps-backend/api/prisma/global/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// This file is read by Prisma's prismaSchemaFolder feature.
// It declares the generator, datasource, and all shared enums for GlobalReferenceDB.
// Models live in the sibling domain files.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/.prisma/global-reference-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("GLOBAL_REFERENCE_DATABASE_URL")
}

// ─── Enums ────────────────────────────────────────────────────────────────────

enum GstRateType {
  STANDARD   // Normal GST slab (0%, 5%, 12%, 18%, 28%)
  CESS       // Additional cess on top of GST (luxury, sin goods)
  EXEMPT     // Fully exempt from GST
  ZERO_RATED // 0% GST but eligible for input tax credit (exports)
}

enum HsnCodeType {
  HSN  // Harmonised System of Nomenclature — goods
  SAC  // Services Accounting Code — services
}
```

Models: 0
0

### apps-backend/api/prisma/global/v1/indian-reference.prisma

```prisma
// GlobalReferenceDB — Indian reference data
// Sprint B.2 — 2026-04-16
// Models: GlCfgPincode, GlCfgHsnCode, GlCfgGstRate

// ─── Indian PIN Codes ─────────────────────────────────────────────────────────

model GlCfgPincode {
  id             String      @id @default(uuid())
  pincode        String      @unique               // 6-digit PIN e.g. "400001"
  stateId        String?     @map("state_id")      // FK → GlCfgState (nullable for non-India)
  district       String?                           // "Mumbai City"
  cityName       String?     @map("city_name")     // "Mumbai"
  officeName     String?     @map("office_name")   // "Mumbai GPO"
  officeType     String?     @map("office_type")   // "H.O.", "S.O.", "B.O."
  deliveryStatus String?     @map("delivery_status") // "Delivery", "Non-Delivery"
  isActive       Boolean     @default(true) @map("is_active")
  createdAt      DateTime    @default(now()) @map("created_at")
  updatedAt      DateTime    @updatedAt @map("updated_at")

  state          GlCfgState? @relation(fields: [stateId], references: [id])

  @@index([pincode])
  @@index([stateId])
  @@index([district])
  @@map("gl_cfg_pincode")
}

// ─── GST Rates ───────────────────────────────────────────────────────────────

model GlCfgGstRate {
  id          String        @id @default(uuid())
  name        String        @unique             // "GST 18%", "GST Exempt", "GST 28% + Cess 12%"
  rate        Decimal       @db.Decimal(5, 2)  // 18.00, 0.00, 28.00
  cessRate    Decimal?      @map("cess_rate") @db.Decimal(5, 2) // 12.00 for luxury goods
  type        GstRateType   @default(STANDARD)
  description String?
  isActive    Boolean       @default(true) @map("is_active")
  sortOrder   Int           @default(0) @map("sort_order")
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")
```

Models: 3

### apps-backend/api/prisma/global/v1/reference.prisma

```prisma
// GlobalReferenceDB — reference domain
// Generated by scripts/split-schemas.py
// Models: GlCfgCountry, GlCfgState, GlCfgCity, GlCfgLookupType, GlCfgLookupValue

model GlCfgCountry {
  id           String       @id @default(uuid())
  code         String       @unique                // ISO-3166 alpha-2: "IN", "US"
  code3        String       @unique                // ISO-3166 alpha-3: "IND", "USA"
  name         String
  nameLocal    String?      @map("name_local")     // Local script name: "भारत"
  phoneCode    String?      @map("phone_code")     // "+91"
  currencyCode String?      @map("currency_code")  // "INR"
  isActive     Boolean      @default(true) @map("is_active")
  sortOrder    Int          @default(0) @map("sort_order")
  createdAt    DateTime     @default(now()) @map("created_at")
  updatedAt    DateTime     @updatedAt @map("updated_at")

  states       GlCfgState[]

  @@index([isActive])
  @@map("gl_cfg_country")
}

model GlCfgState {
  id           String       @id @default(uuid())
  countryId    String       @map("country_id")
  code         String                               // "MH" for Maharashtra
  name         String
  nameLocal    String?      @map("name_local")      // "महाराष्ट्र"
  gstStateCode String?      @map("gst_state_code")  // "27" — Indian GST state code
  isActive     Boolean      @default(true) @map("is_active")
  sortOrder    Int          @default(0) @map("sort_order")
  createdAt    DateTime     @default(now()) @map("created_at")
  updatedAt    DateTime     @updatedAt @map("updated_at")

  country      GlCfgCountry  @relation(fields: [countryId], references: [id])
  cities       GlCfgCity[]
  pincodes     GlCfgPincode[]

  @@unique([countryId, code])
```

Models: 5

### apps-backend/api/prisma/global/v1/system-reference.prisma

```prisma
// GlobalReferenceDB — System reference data
// Sprint B.2 — 2026-04-16
// Models: GlCfgCurrency, GlCfgTimezone, GlCfgIndustryType, GlCfgLanguage

// ─── Currencies ───────────────────────────────────────────────────────────────

model GlCfgCurrency {
  id             String   @id @default(uuid())
  code           String   @unique             // ISO 4217: "INR", "USD", "EUR"
  name           String                       // "Indian Rupee"
  nameLocal      String?  @map("name_local")  // "भारतीय रुपया"
  symbol         String                       // "₹", "$", "€"
  symbolAlt      String?  @map("symbol_alt")  // "Rs.", "US$"
  decimalPlaces  Int      @default(2) @map("decimal_places")
  thousandSep    String   @default(",") @map("thousand_sep") // "," or "."
  decimalSep     String   @default(".") @map("decimal_sep")  // "." or ","
  isDefault      Boolean  @default(false) @map("is_default") // true for INR
  isActive       Boolean  @default(true) @map("is_active")
  sortOrder      Int      @default(0) @map("sort_order")
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  @@index([isActive])
  @@index([isDefault])
  @@map("gl_cfg_currency")
}

// ─── Timezones ────────────────────────────────────────────────────────────────

model GlCfgTimezone {
  id             String   @id @default(uuid())
  tzIdentifier   String   @unique @map("tz_identifier") // IANA: "Asia/Kolkata"
  displayName    String   @map("display_name")          // "(UTC+05:30) India Standard Time"
  offsetHours    Int      @map("offset_hours")          // 5
  offsetMinutes  Int      @default(0) @map("offset_minutes") // 30
  isDst          Boolean  @default(false) @map("is_dst") // true if observes daylight saving
  isDefault      Boolean  @default(false) @map("is_default") // true for Asia/Kolkata
  isActive       Boolean  @default(true) @map("is_active")
  sortOrder      Int      @default(0) @map("sort_order")
  createdAt      DateTime @default(now()) @map("created_at")
```

Models: 4

### apps-backend/api/prisma/identity/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// This file is read by Prisma's prismaSchemaFolder feature.
// It declares the generator, datasource, and all shared enums for IdentityDB.
// Models live in the sibling domain files.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/@prisma/identity-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("IDENTITY_DATABASE_URL")
}

// ─── Enums ─────────────────────────────────────────────────────────────────

enum UserStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
}

enum VerificationStatus {
  UNVERIFIED
  PARTIALLY_VERIFIED
  FULLY_VERIFIED
}

enum RegistrationType {
  INDIVIDUAL
  BUSINESS
}

enum OtpPurpose {
  EMAIL_VERIFICATION
  MOBILE_VERIFICATION
  PASSWORD_RESET
  LOGIN_OTP
```

Models: 0
0

### apps-backend/api/prisma/identity/v1/audit.prisma

```prisma
// IdentityDB — audit domain
// Generated by scripts/split-schemas.py
// Models: AuditLog, AuditFieldChange, AuditRetentionPolicy, CredentialAccessLog, TenantAuditSession, TenantAuditLog, CustomerPortalLog

model AuditLog {
  id       String @id @default(uuid())
  tenantId String @default("") @map("tenant_id")

  // What changed
  entityType  AuditEntityType @map("entity_type")
  entityId    String          @map("entity_id")
  entityLabel String?         @map("entity_label")
  action      AuditAction

  // Summary
  summary     String
  changeCount Int    @default(0) @map("change_count")

  // Snapshots
  beforeSnapshot Json? @map("before_snapshot")
  afterSnapshot  Json? @map("after_snapshot")

  // Who
  performedById    String? @map("performed_by_id")
  performedByName  String? @map("performed_by_name")
  performedByEmail String? @map("performed_by_email")
  performedByRole  String? @map("performed_by_role")

  // Request context
  ipAddress   String? @map("ip_address")
  userAgent   String? @map("user_agent")
  httpMethod  String? @map("http_method")
  requestUrl  String? @map("request_url")
  requestBody Json?   @map("request_body")

  // Source
  source        String?
  module        String?
  correlationId String? @map("correlation_id")

```

Models: 7

### apps-backend/api/prisma/identity/v1/config.prisma

```prisma
// IdentityDB — config domain
// Generated by scripts/split-schemas.py
// Models: Menu, TenantConfig, TenantCredential, GlobalDefaultCredential, TenantBranding, SecurityPolicy, IpAccessRule, DataRetentionPolicy, TerminologyOverride, TenantVersion, VersionBackup, CustomerMenuCategory

model Menu {
  id                        String               @id @default(uuid())
  tenantId                  String               @default("") @map("tenant_id")
  name                      String
  code                      String
  icon                      String?
  route                     String?
  parentId                  String?              @map("parent_id")
  parent                    Menu?                @relation("MenuHierarchy", fields: [parentId], references: [id])
  children                  Menu[]               @relation("MenuHierarchy")
  sortOrder                 Int                  @default(0) @map("sort_order")
  menuType                  String               @default("ITEM") @map("menu_type")
  permissionModule          String?              @map("permission_module")
  permissionAction          String?              @map("permission_action")
  badgeColor                String?              @map("badge_color")
  badgeText                 String?              @map("badge_text")
  openInNewTab              Boolean              @default(false) @map("open_in_new_tab")
  isActive                  Boolean              @default(true) @map("is_active")
  requiresCredential        Boolean              @default(false) @map("requires_credential")
  credentialKey             String?              @map("credential_key")
  businessTypeApplicability Json                 @default("[\"ALL\"]") @map("business_type_applicability")
  autoEnableWithModule      String?              @map("auto_enable_with_module")
  terminologyKey            String?              @map("terminology_key")
  industryCode              String?              @map("industry_code")
  isAdminOnly               Boolean              @default(false) @map("is_admin_only")
  createdAt                 DateTime             @default(now()) @map("created_at")
  updatedAt                 DateTime             @updatedAt @map("updated_at")
  roleMenuPermissions       RoleMenuPermission[]

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
```

Models: 12

### apps-backend/api/prisma/identity/v1/licensing.prisma

```prisma
// IdentityDB — licensing domain
// Generated by scripts/split-schemas.py
// Models: Plan, Subscription, TenantInvoice, TenantUsage, PlanLimit, PlanModuleAccess

model Plan {
  id          String        @id @default(uuid())
  name        String        @unique
  code        String        @unique
  description String?
  interval    PlanInterval  @default(MONTHLY)
  price       Decimal       @db.Decimal(12, 2)
  currency    String        @default("INR")
  maxUsers    Int           @default(3) @map("max_users")
  maxContacts Int           @default(100) @map("max_contacts")
  maxLeads    Int           @default(50) @map("max_leads")
  maxProducts Int           @default(20) @map("max_products")
  maxStorage  Int           @default(100) @map("max_storage")
  features    FeatureFlag[]
  isActive    Boolean       @default(true) @map("is_active")
  sortOrder   Int           @default(0) @map("sort_order")
  configJson  Json?         @map("config_json")
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")

  subscriptions Subscription[]
  limits        PlanLimit[]
  moduleAccess  PlanModuleAccess[]

  @@map("gv_lic_plans")
}

model Subscription {
  id                 String             @id @default(uuid())
  tenantId           String             @default("") @map("tenant_id")
  planId             String             @map("plan_id")
  status             SubscriptionStatus @default(TRIALING)
  currentPeriodStart DateTime?          @map("current_period_start")
  currentPeriodEnd   DateTime?          @map("current_period_end")
  trialEndsAt        DateTime?          @map("trial_ends_at")
  cancelledAt        DateTime?          @map("cancelled_at")
```

Models: 6

### apps-backend/api/prisma/identity/v1/users.prisma

```prisma
// IdentityDB — users domain
// Generated by scripts/split-schemas.py
// Models: User, CustomerProfile, ReferralPartner, Department, Designation, Role, Permission, RolePermission, RoleMenuPermission, PermissionTemplate, UserCapacity, DelegationRecord, UserPermissionOverride, Tenant, SuperAdmin, TenantProfile, VerificationOtp, CustomerUser

model User {
  id            String       @id @default(uuid())
  tenantId      String       @default("") @map("tenant_id")
  email         String
  password      String
  firstName     String       @map("first_name")
  lastName      String       @map("last_name")
  phone         String?
  avatar        String?
  status        UserStatus   @default(ACTIVE)
  userType      UserType     @default(EMPLOYEE) @map("user_type")
  lastLoginAt   DateTime?    @map("last_login_at")
  roleId        String       @map("role_id")
  role          Role         @relation(fields: [roleId], references: [id])
  departmentId  String?      @map("department_id")
  department    Department?  @relation("UserDepartment", fields: [departmentId], references: [id])
  designationId String?      @map("designation_id")
  designation   Designation? @relation(fields: [designationId], references: [id])
  reportingToId String?      @map("reporting_to_id")
  reportingTo   User?        @relation("UserReporting", fields: [reportingToId], references: [id])
  reportees     User[]       @relation("UserReporting")
  employeeCode  String?      @map("employee_code")
  joiningDate   DateTime?    @map("joining_date")
  isDeleted     Boolean      @default(false) @map("is_deleted")
  deletedAt     DateTime?    @map("deleted_at")
  deletedById   String?      @map("deleted_by_id")
  createdAt     DateTime     @default(now()) @map("created_at")
  updatedAt     DateTime     @updatedAt @map("updated_at")
  createdBy     String?      @map("created_by")

  // ─── VERIFICATION STATUS ───
  verificationStatus VerificationStatus @default(UNVERIFIED) @map("verification_status")
  emailVerified      Boolean            @default(false) @map("email_verified")
  emailVerifiedAt    DateTime?          @map("email_verified_at")
  mobileVerified     Boolean            @default(false) @map("mobile_verified")
  mobileVerifiedAt   DateTime?          @map("mobile_verified_at")
```

Models: 18

### apps-backend/api/prisma/marketplace/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// This file is read by Prisma's prismaSchemaFolder feature.
// It declares the generator, datasource, and all shared enums for MarketplaceDB.
// Models live in the sibling domain files.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/@prisma/marketplace-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("MARKETPLACE_DATABASE_URL")
}

// ─── Enums ─────────────────────────────────────────────────────────────────

enum ListingType {
  PRODUCT
  SERVICE
  REQUIREMENT
  JOB
}

enum ListingStatus {
  DRAFT
  SCHEDULED
  ACTIVE
  PAUSED
  SOLD_OUT
  EXPIRED
  ARCHIVED
  REJECTED
}

enum PostType {
  TEXT
  IMAGE
  VIDEO
```

Models: 0
0

### apps-backend/api/prisma/marketplace/v1/marketplace.prisma

```prisma
// MarketplaceDB — marketplace domain
// Generated by scripts/split-schemas.py
// Models: MktListing, MktListingPriceTier, MktPost, MktPostEngagement, MktPostComment, MktOffer, MktOfferRedemption, MktReview, MktEnquiry, MktAnalyticsEvent, MktAnalyticsSummary, MktFollow, MktRequirementQuote

model MktListing {
  id                  String          @id @default(uuid())
  tenantId            String          @map("tenant_id")
  authorId            String          @map("author_id")
  listingType         ListingType     @map("listing_type")
  categoryId          String?         @map("category_id")
  subcategoryId       String?         @map("subcategory_id")
  title               String
  description         String?         @db.Text
  shortDescription    String?         @map("short_description")
  mediaUrls           Json            @default("[]") @map("media_urls")
  currency            String          @default("INR")
  basePrice           Int             @default(0) @map("base_price")
  mrp                 Int?
  minOrderQty         Int             @default(1) @map("min_order_qty")
  maxOrderQty         Int?            @map("max_order_qty")
  hsnCode             String?         @map("hsn_code")
  gstRate             Float?          @map("gst_rate")
  trackInventory      Boolean         @default(true) @map("track_inventory")
  stockAvailable      Int             @default(0) @map("stock_available")
  stockReserved       Int             @default(0) @map("stock_reserved")
  visibility          VisibilityType  @default(PUBLIC)
  visibilityConfig    Json?           @map("visibility_config")
  status              ListingStatus   @default(DRAFT)
  publishAt           DateTime?       @map("publish_at")
  expiresAt           DateTime?       @map("expires_at")
  expiryAction        ExpiryAction    @default(DEACTIVATE) @map("expiry_action")
  publishedAt         DateTime?       @map("published_at")
  requirementConfig   Json?           @map("requirement_config")
  shippingConfig      Json?           @map("shipping_config")
  attributes          Json            @default("{}") @map("attributes")
  viewCount           Int             @default(0) @map("view_count")
  enquiryCount        Int             @default(0) @map("enquiry_count")
  orderCount          Int             @default(0) @map("order_count")
  reviewCount         Int             @default(0) @map("review_count")
  avgRating           Float?          @map("avg_rating")
```

Models: 13

### apps-backend/api/prisma/platform-console/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// This file is read by Prisma's prismaSchemaFolder feature.
// It declares the generator, datasource, and all shared enums for PlatformConsoleDB.
// Models live in the sibling domain files.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/.prisma/platform-console-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("PLATFORM_CONSOLE_DATABASE_URL")
}
```

Models: 0
0

### apps-backend/api/prisma/platform-console/v1/devops.prisma

```prisma
// PlatformConsoleDB — devops domain
// Generated by scripts/split-schemas.py
// Models: GlobalErrorLog, CustomerErrorReport, ErrorEscalation, ErrorAutoReport, ErrorTrend, AlertRule, VersionRelease, VerticalVersion, RollbackLog, VerticalRegistry, VerticalAudit, VerticalHealth, BrandModuleWhitelist, BrandFeatureFlag, BrandErrorSummary, HealthSnapshot, AlertHistory, IncidentLog, NotificationLog, DRPlan, DeploymentLog, PipelineRun, BuildLog, GlobalMenuConfig, BrandMenuOverride

model GlobalErrorLog {
  id             String   @id @default(uuid())
  brandId        String?  @map("brand_id")
  tenantId       String?  @map("tenant_id")
  verticalType   String?  @map("vertical_type")
  severity       String   // CRITICAL | HIGH | MEDIUM | LOW
  errorCode      String   @map("error_code")
  message        String
  stackTrace     String?  @map("stack_trace") @db.Text
  module         String?
  component      String?
  endpoint       String?
  httpStatus     Int?     @map("http_status")
  requestContext Json?    @map("request_context")
  userId         String?  @map("user_id")
  userAgent      String?  @map("user_agent")
  ipAddress      String?  @map("ip_address")
  resolvedAt     DateTime? @map("resolved_at")
  resolvedBy     String?  @map("resolved_by")
  resolution     String?
  createdAt      DateTime @default(now()) @map("created_at")

  escalation     ErrorEscalation?

  @@index([severity])
  @@index([brandId])
  @@index([verticalType])
  @@index([createdAt])
  @@index([errorCode])
  @@map("pc_error_global_logs")
}

model CustomerErrorReport {
  id           String    @id @default(uuid())
  brandId      String    @map("brand_id")
  tenantId     String?   @map("tenant_id")
```

Models: 25

### apps-backend/api/prisma/platform-console/v1/testing.prisma

```prisma
// PlatformConsoleDB — testing domain
// Generated by scripts/split-schemas.py
// Models: PcTestPlan, PcTestSchedule, PcTestExecution, PcTestCoverage

model PcTestPlan {
  id            String   @id @default(uuid())
  name          String
  description   String?
  moduleScope   String?  @map("module_scope")
  verticalScope String?  @map("vertical_scope")
  scenarios     Json     @default("[]")
  createdBy     String   @map("created_by")
  isActive      Boolean  @default(true) @map("is_active")
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")

  executions    PcTestExecution[]

  @@map("pc_test_plans")
}

model PcTestSchedule {
  id             String    @id @default(uuid())
  planId         String?   @map("plan_id")
  scheduleType   String    @map("schedule_type")
  cronExpression String?   @map("cron_expression")
  moduleScope    String?   @map("module_scope")
  verticalScope  String?   @map("vertical_scope")
  brandScope     String?   @map("brand_scope")
  isActive       Boolean   @default(true) @map("is_active")
  lastRun        DateTime? @map("last_run")
  nextRun        DateTime? @map("next_run")
  createdAt      DateTime  @default(now()) @map("created_at")

  @@map("pc_test_schedules")
}

model PcTestExecution {
  id            String    @id @default(uuid())
  planId        String?   @map("plan_id")
```

Models: 4

### apps-backend/api/prisma/platform/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// This file is read by Prisma's prismaSchemaFolder feature.
// It declares the generator, datasource, and all shared enums for PlatformDB.
// Models live in the sibling domain files.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/@prisma/platform-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("PLATFORM_DATABASE_URL")
}

// ─── Enums ─────────────────────────────────────────────────────────────────

enum ContactType {
  RAW
  VALIDATED
}

enum ContactSource {
  MANUAL
  BULK_IMPORT
  WEB_FORM
  REFERRAL
  API
}

enum RuleConditionOperator {
  EQUALS
  NOT_EQUALS
  CONTAINS
  GREATER_THAN
  LESS_THAN
  IN
  NOT_IN
  IS_EMPTY
```

Models: 0
0

### apps-backend/api/prisma/platform/v1/audit.prisma

```prisma
// PlatformDB — audit domain
// Generated by scripts/split-schemas.py
// Models: ErrorLog, ErrorAutoReportRule, TenantActivityLog, PluginHookLog

model ErrorLog {
  id          String        @id @default(uuid())
  requestId   String        @map("request_id")
  errorCode   String        @map("error_code")
  message     String        @db.Text
  statusCode  Int           @map("status_code")
  layer       ErrorLayer    @default(BE)
  severity    ErrorSeverity @default(ERROR)
  path        String
  method      String
  userId      String?       @map("user_id")
  tenantId    String?       @map("tenant_id")
  details     Json?
  stack       String?       @db.Text
  ip          String?
  userAgent   String?       @map("user_agent") @db.VarChar(500)
  module      String?       @db.VarChar(100)
  requestBody Json?         @map("request_body")
  queryParams Json?         @map("query_params")
  metadata    Json?

  // ─── ENHANCED: Request/Response capture ───
  requestHeaders Json? @map("request_headers")
  responseBody   Json? @map("response_body")
  responseTimeMs Int?  @map("response_time_ms")

  // ─── ENHANCED: User/Tenant context ───
  userName     String? @map("user_name")
  userRole     String? @map("user_role")
  tenantName   String? @map("tenant_name")
  industryCode String? @map("industry_code")

  // ─── ENHANCED: Auto-report ───
  isAutoReported Boolean   @default(false) @map("is_auto_reported")
  autoReportedAt DateTime? @map("auto_reported_at")
  autoReportedTo String[]  @default([]) @map("auto_reported_to")
```

Models: 4

### apps-backend/api/prisma/platform/v1/marketplace.prisma

```prisma
// PlatformDB — marketplace domain
// Generated by scripts/split-schemas.py
// Models: MarketplaceModule, MarketplaceReview, TenantMarketplaceModule, MarketplaceListing, ListingPriceTier, MarketplacePost, PostEngagement, PostComment, MarketplaceEnquiry, MarketplaceOrder, MarketplaceOrderItem, ListingAnalytics, PostAnalytics

model MarketplaceModule {
  id               String                  @id @default(uuid())
  vendorId         String                  @map("vendor_id")
  moduleCode       String                  @unique @map("module_code")
  moduleName       String                  @map("module_name")
  category         String
  shortDescription String                  @map("short_description")
  longDescription  String                  @map("long_description") @db.Text
  screenshots      Json                    @default("[]")
  demoVideoUrl     String?                 @map("demo_video_url")
  documentationUrl String?                 @map("documentation_url")
  changelog        Json                    @default("[]")
  version          String                  @default("1.0.0")
  status           MarketplaceModuleStatus @default(DRAFT)
  publishedAt      DateTime?               @map("published_at")
  launchOfferDays  Int?                    @map("launch_offer_days")
  launchOfferEnds  DateTime?               @map("launch_offer_ends")
  pricingPlans     Json                    @default("[]") @map("pricing_plans")
  usageLimits      Json                    @default("{}") @map("usage_limits")
  targetTypes      Json                    @default("[\"ALL\"]") @map("target_types")
  installCount     Int                     @default(0) @map("install_count")
  avgRating        Decimal                 @default(0) @map("avg_rating") @db.Decimal(3, 2)
  reviewCount      Int                     @default(0) @map("review_count")
  createdAt        DateTime                @default(now()) @map("created_at")
  updatedAt        DateTime                @updatedAt @map("updated_at")

  vendor        MarketplaceVendor         @relation(fields: [vendorId], references: [id], onDelete: Cascade)
  reviews       MarketplaceReview[]
  installations TenantMarketplaceModule[]

  // AI Apps Marketplace extensions
  moduleType             ModuleType    @default(INTEGRATION) @map("module_type")
  capabilities           String[]      @default([])
  aiTaskTypes            String[]      @default([]) @map("ai_task_types")
  aiHandlerKey           String?       @map("ai_handler_key")
  aiHandlerType          AiHandlerType @default(GENERIC) @map("ai_handler_type")
```

Models: 13

### apps-backend/api/prisma/platform/v1/modules.prisma

```prisma
// PlatformDB — modules domain
// Generated by scripts/split-schemas.py
// Models: MasterLookup, LookupValue, ErrorCatalog, ModuleDefinition, BusinessTypeRegistry, IndustryPackage, TenantModule, HelpArticle, PluginRegistry, TenantPlugin, PageRegistry, AppVersion, IndustryPatch, DatabaseBackupRecord, BackupLog, RestoreLog, SystemFieldMaster, GvCfgVertical, GvCfgVerticalModule

model MasterLookup {
  id          String        @id @default(uuid())
  tenantId    String        @default("") @map("tenant_id")
  category    String
  displayName String        @map("display_name")
  description String?
  isSystem    Boolean       @default(false) @map("is_system")
  isActive    Boolean       @default(true) @map("is_active")
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")
  values      LookupValue[]

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@unique([tenantId, category])
  @@index([tenantId])
  @@map("gv_cfg_master_lookups")
}

model LookupValue {
  id         String        @id @default(uuid())
  tenantId   String        @default("") @map("tenant_id")
  lookupId   String        @map("lookup_id")
  value      String
  label      String
  icon       String?
  color      String?
  rowIndex   Int           @default(0) @map("row_index")
  isDefault  Boolean       @default(false) @map("is_default")
  isActive   Boolean       @default(true) @map("is_active")
  parentId   String?       @map("parent_id")
```

Models: 19

### apps-backend/api/prisma/platform/v1/subscriptions.prisma

```prisma
// PlatformDB — subscriptions domain
// Generated by scripts/split-schemas.py
// Models: Package, TenantUsageDetail, Wallet, WalletTransaction, RechargePlan, Coupon, SoftwareOffer, LicenseKey, AiPlan, AiSubscription, AiSubscriptionItem, SubscriptionPackage, CouponRedemption, PackageModule

model Package {
  id          String   @id @default(uuid())
  tenantId    String   @default("") @map("tenant_id")
  name        String
  code        String
  description String?
  type        String?
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@unique([tenantId, name])
  @@unique([tenantId, code])
  @@index([tenantId])
  @@map("gv_lic_packages")
}

model TenantUsageDetail {
  id           String   @id @default(uuid())
  tenantId     String   @map("tenant_id")
  resourceKey  String   @map("resource_key")
  currentCount Int      @default(0) @map("current_count")
  monthlyCount Int      @default(0) @map("monthly_count")
  monthYear    String?  @map("month_year")
  lastUpdated  DateTime @default(now()) @map("last_updated")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
```

Models: 14

### apps-backend/api/prisma/platform/v1/testing.prisma

```prisma
// PlatformDB — testing domain
// Generated by scripts/split-schemas.py
// Models: TestEnvironment, TestRun, TestResult, TestGroup, TestGroupExecution, ManualTestLog, ScheduledTest, ScheduledTestRun, TestPlan, TestPlanItem, TestEvidence, TestErrorLog, TestReport

model TestEnvironment {
  id          String @id @default(uuid())
  tenantId    String @map("tenant_id")

  name        String
  displayName String @map("display_name")

  sourceType  TestEnvSourceType @map("source_type")
  sourceDbUrl String?           @map("source_db_url")
  backupId    String?           @map("backup_id")

  testDbName  String  @map("test_db_name")
  testDbUrl   String? @map("test_db_url")

  status          TestEnvStatus @default(QUEUED)
  statusMessage   String?       @map("status_message")
  progressPercent Int           @default(0) @map("progress_percent")

  dbSizeBytes  BigInt? @map("db_size_bytes")
  tablesCreated Int?   @map("tables_created")
  seedRecords   Int?   @map("seed_records")

  ttlHours  Int       @default(24) @map("ttl_hours")
  expiresAt DateTime? @map("expires_at")

  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")
  createdById String    @map("created_by_id")
  completedAt DateTime? @map("completed_at")
  cleanedAt   DateTime? @map("cleaned_at")

  errorMessage String? @map("error_message") @db.Text
  errorStack   String? @map("error_stack") @db.Text

  @@index([tenantId])
  @@index([status])
```

Models: 13

### apps-backend/api/prisma/platform/v1/vendors.prisma

```prisma
// PlatformDB — vendors domain
// Generated by scripts/split-schemas.py
// Models: MarketplaceVendor

model MarketplaceVendor {
  id              String       @id @default(uuid())
  companyName     String       @map("company_name")
  contactName     String?      @map("contact_name")
  contactEmail    String       @unique @map("contact_email")
  password        String?
  gstNumber       String?      @map("gst_number")
  bankAccountEnc  String?      @map("bank_account_enc") @db.Text
  revenueSharePct Decimal      @default(70) @map("revenue_share_pct") @db.Decimal(5, 2)
  status          VendorStatus @default(PENDING)
  verifiedAt      DateTime?    @map("verified_at")
  lastLoginAt     DateTime?    @map("last_login_at")
  createdAt       DateTime     @default(now()) @map("created_at")
  updatedAt       DateTime     @updatedAt @map("updated_at")

  modules MarketplaceModule[]

  // AI Apps Marketplace extensions
  vendorType    VendorType @default(PLATFORM_OWNER) @map("vendor_type")
  aiPluginCount Int        @default(0) @map("ai_plugin_count")
  supportEmail  String?    @map("support_email")
  websiteUrl    String?    @map("website_url")

  @@map("gv_ven_marketplace_vendors")
}
```

Models: 1

### apps-backend/api/prisma/working/v1/_base.prisma

```prisma
// ─── Generator + Datasource ─────────────────────────────────────────────────
// This file is read by Prisma's prismaSchemaFolder feature.
// It declares the generator, datasource, and all shared enums for WorkingDB.
// Models live in the sibling domain files.

generator client {
  provider        = "prisma-client-js"
  output          = "../../../node_modules/@prisma/working-client"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = env("GLOBAL_WORKING_DATABASE_URL")
}

// ─── Enums ─────────────────────────────────────────────────────────────────

enum RawContactStatus {
  RAW
  VERIFIED
  REJECTED
  DUPLICATE
}

enum RawContactSource {
  MANUAL
  BULK_IMPORT
  WEB_FORM
  REFERRAL
  API
}

enum CommunicationType {
  PHONE
  EMAIL
  MOBILE
  ADDRESS
  WHATSAPP
}
```

Models: 0
0

### apps-backend/api/prisma/working/v1/accounts.prisma

```prisma
// WorkingDB — accounts domain
// Generated by scripts/split-schemas.py
// Models: Invoice, InvoiceLineItem, ProformaInvoice, ProformaLineItem, CreditNote, SavedFormula, PurchaseInvoice, PurchaseInvoiceItem, LedgerMaster, LedgerMapping, AccountTransaction, BankAccount, BankReconciliation, DebitNote, DebitNoteItem, AccountGroup

model Invoice {
  id        String        @id @default(uuid())
  tenantId  String        @default("") @map("tenant_id")
  invoiceNo String        @map("invoice_no")
  status    InvoiceStatus @default(DRAFT)

  // ─── SOURCE ───
  quotationId       String?    @map("quotation_id")
  quotation         Quotation? @relation(fields: [quotationId], references: [id])
  leadId            String?    @map("lead_id")
  lead              Lead?      @relation(fields: [leadId], references: [id])
  saleOrderId       String?    @map("sale_order_id")
  deliveryChallanId String?    @map("delivery_challan_id")
  inventoryEffect   Boolean    @default(true) @map("inventory_effect")
  accountsEffect    Boolean    @default(true) @map("accounts_effect")

  // ─── CUSTOMER ───
  contactId      String?       @map("contact_id")
  contact        Contact?      @relation(fields: [contactId], references: [id])
  organizationId String?       @map("organization_id")
  organization   Organization? @relation(fields: [organizationId], references: [id])

  // ─── BILLING INFO ───
  billingName      String  @map("billing_name")
  billingAddress   String? @map("billing_address")
  billingCity      String? @map("billing_city")
  billingState     String? @map("billing_state")
  billingPincode   String? @map("billing_pincode")
  billingGstNumber String? @map("billing_gst_number")
  shippingName     String? @map("shipping_name")
  shippingAddress  String? @map("shipping_address")
  shippingCity     String? @map("shipping_city")
  shippingState    String? @map("shipping_state")
  shippingPincode  String? @map("shipping_pincode")

  // ─── SELLER INFO (snapshot) ───
```

Models: 16

### apps-backend/api/prisma/working/v1/audit.prisma

```prisma
// WorkingDB — audit domain
// Generated by scripts/split-schemas.py
// Models: SyncChangeLog, SyncAuditLog, CronJobRunLog, ApiRequestLog, UnmaskAuditLog, AiUsageLog, ControlRoomAuditLog

model SyncChangeLog {
  id String @id @default(uuid())

  deviceId String @map("device_id")
  userId   String @map("user_id")

  // ─── CHANGE ───
  entityName String       @map("entity_name")
  entityId   String       @map("entity_id")
  action     ChangeAction

  changedFields  Json? @map("changed_fields")
  fullRecord     Json? @map("full_record")
  previousValues Json? @map("previous_values")

  clientTimestamp DateTime @map("client_timestamp")
  clientVersion   Int      @default(1) @map("client_version")

  // ─── SYNC STATE ───
  isPushed     Boolean   @default(false) @map("is_pushed")
  pushedAt     DateTime? @map("pushed_at")
  pushResult   String?   @map("push_result")
  conflictId   String?   @map("conflict_id")
  errorMessage String?   @map("error_message")

  tenantId  String   @default("") @map("tenant_id")
  createdAt DateTime @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([userId, deviceId, isPushed])
```

Models: 7

### apps-backend/api/prisma/working/v1/communication.prisma

```prisma
// WorkingDB — communication domain
// Generated by scripts/split-schemas.py
// Models: EmailAccount, EmailThread, Email, EmailAttachment, EmailTemplate, EmailSignature, EmailCampaign, CampaignRecipient, EmailTrackingEvent, EmailUnsubscribe, WhatsAppBusinessAccount, WaConversation, WaMessage, WaTemplate, WaBroadcast, WaBroadcastRecipient, WaChatbotFlow, WaQuickReply, WaOptOut, EmailFooterTemplate, CommunicationLog

model EmailAccount {
  id       String @id @default(uuid())
  tenantId String @default("") @map("tenant_id")
  userId   String @map("user_id")
  // [cross-db removed] user     User   @relation("EmailAccountUser", fields: [userId], references: [id])

  provider     EmailProvider
  label        String?
  emailAddress String        @map("email_address")
  displayName  String?       @map("display_name")

  accessToken    String?   @map("access_token")
  refreshToken   String?   @map("refresh_token")
  tokenExpiresAt DateTime? @map("token_expires_at")
  oauthScope     String?   @map("oauth_scope")

  imapHost     String?  @map("imap_host")
  imapPort     Int?     @map("imap_port")
  imapSecure   Boolean? @map("imap_secure")
  smtpHost     String?  @map("smtp_host")
  smtpPort     Int?     @map("smtp_port")
  smtpSecure   Boolean? @map("smtp_secure")
  smtpUsername String?  @map("smtp_username")
  smtpPassword String?  @map("smtp_password")

  status        EmailAccountStatus @default(ACTIVE)
  lastSyncAt    DateTime?          @map("last_sync_at")
  lastSyncError String?            @map("last_sync_error")
  syncToken     String?            @map("sync_token")
  syncFromDate  DateTime?          @map("sync_from_date")
  syncEnabled   Boolean            @default(true) @map("sync_enabled")

  isDefault       Boolean @default(false) @map("is_default")
  autoLinkEnabled Boolean @default(true) @map("auto_link_enabled")
  signatureId     String? @map("signature_id")

```

Models: 21

### apps-backend/api/prisma/working/v1/config.prisma

```prisma
// WorkingDB — config domain
// Generated by scripts/split-schemas.py
// Models: Brand, BrandOrganization, BrandContact, BusinessLocation, CompanyCountry, CompanyState, CompanyCity, CompanyPincode, CustomFieldDefinition, EntityConfigValue, SyncPolicy, SyncWarningRule, SyncDevice, SyncConflict, SyncFlushCommand, CronJobConfig, BusinessHoursSchedule, HolidayCalendar, CalendarHighlight, AutoNumberSequence, CompanyProfile, NotionConfig, ApiKey, WebhookEndpoint, WebhookDelivery, RateLimitTier, TableConfig, DataMaskingPolicy, QuietHourConfig, UserCalendarSync, UserAvailability, BlockedSlot, CalendarConfig, GoogleConnection, AiSettings, ShortcutDefinition, ShortcutUserOverride, AiModel, AiDataset, AiDocument, AiTrainingJob, AiEmbedding, AiChatSession, AiChatMessage, AiSystemPrompt, ControlRoomRule, ControlRoomValue, TenantRuleCacheVersion, ControlRoomDraft, SavedFilter

model Brand {
  id                 String              @id @default(uuid())
  tenantId           String              @default("") @map("tenant_id")
  name               String
  code               String
  description        String?
  logo               String?
  website            String?
  isActive           Boolean             @default(true) @map("is_active")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  brandOrganizations BrandOrganization[]
  brandContacts      BrandContact[]
  products           Product[]

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@unique([tenantId, name])
  @@unique([tenantId, code])
  @@index([tenantId])
  @@map("gv_cfg_brands")
}

model BrandOrganization {
  id             String       @id @default(uuid())
  tenantId       String       @default("") @map("tenant_id")
  brandId        String       @map("brand_id")
  brand          Brand        @relation(fields: [brandId], references: [id], onDelete: Cascade)
  organizationId String       @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  isPrimary      Boolean      @default(false) @map("is_primary")
```

Models: 50

### apps-backend/api/prisma/working/v1/crm.prisma

```prisma
// WorkingDB — crm domain
// Generated by scripts/split-schemas.py
// Models: RawContact, RawContactFilter, Contact, ContactFilter, Organization, OrganizationFilter, ContactOrganization, Communication, Lead, LeadFilter, Activity, Demo, TourPlan, EntityOwner, OwnershipLog, AssignmentRule, Manufacturer, ManufacturerOrganization, ManufacturerContact, OrganizationLocation, FollowUp, RecurringEvent, TourPlanVisit, TourPlanPhoto, CalendarEvent, SupportTicket, SupportTicketMessage, Task, TaskHistory, TaskWatcher, Comment, TaskLogicConfig, ScheduledEvent, EventParticipant, EventHistory, EntityVerificationRecord, WarrantyTemplate, WarrantyRecord, WarrantyClaim, AMCPlanTemplate, AMCContract, AMCSchedule, ServiceVisitLog, ServiceCharge

model RawContact {
  id            String           @id @default(uuid())
  tenantId      String           @default("") @map("tenant_id")
  source        RawContactSource @default(MANUAL)
  status        RawContactStatus @default(RAW)
  firstName     String           @map("first_name")
  lastName      String           @map("last_name")
  designation   String?
  department    String?
  companyName   String?          @map("company_name")
  notes         String?
  isActive      Boolean          @default(true) @map("is_active")
  isDeleted                 Boolean          @default(false) @map("is_deleted")
  entityVerificationStatus  String           @default("UNVERIFIED") @map("entity_verification_status")
  entityVerifiedVia         String?          @map("entity_verified_via")
  deletedAt     DateTime?        @map("deleted_at")
  deletedById   String?          @map("deleted_by_id")
  verifiedAt    DateTime?        @map("verified_at")
  verifiedById  String?          @map("verified_by_id")
  // [cross-db removed] verifiedBy    User?            @relation("RawContactVerifiedBy", fields: [verifiedById], references: [id])
  contactId     String?          @map("contact_id")
  contact       Contact?         @relation(fields: [contactId], references: [id])
  createdById   String           @map("created_by_id")
  // [cross-db removed] createdByUser User             @relation("RawContactCreatedBy", fields: [createdById], references: [id])
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")

  filters        RawContactFilter[]
  communications Communication[]    @relation("CommRawContact")

  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([status])
  @@index([firstName, lastName])
  @@index([tenantId])
```

Models: 44

### apps-backend/api/prisma/working/v1/documents.prisma

```prisma
// WorkingDB — documents domain
// Generated by scripts/split-schemas.py
// Models: ImportProfile, ImportJob, ImportRow, ExportJob, Document, DocumentAttachment, DocumentFolder, CloudConnection, DocumentShareLink, DocumentActivity, DocumentTemplate, TenantTemplateCustomization

model ImportProfile {
  id           String             @id @default(uuid())
  tenantId     String             @default("") @map("tenant_id")
  name         String
  description  String?
  sourceSystem String?            @map("source_system")
  icon         String?
  color        String?
  targetEntity ImportTargetEntity @map("target_entity")

  expectedHeaders String[] @map("expected_headers")
  headerMatchMode String   @default("FLEXIBLE") @map("header_match_mode")

  fieldMapping    Json  @map("field_mapping")
  defaultValues   Json? @map("default_values")
  validationRules Json? @map("validation_rules")

  duplicateCheckFields String[]          @map("duplicate_check_fields")
  duplicateStrategy    DuplicateStrategy @default(ASK_PER_ROW) @map("duplicate_strategy")
  fuzzyMatchEnabled    Boolean           @default(false) @map("fuzzy_match_enabled")
  fuzzyMatchFields     String[]          @map("fuzzy_match_fields")
  fuzzyThreshold       Decimal           @default(0.85) @map("fuzzy_threshold") @db.Decimal(3, 2)

  usageCount     Int       @default(0) @map("usage_count")
  lastUsedAt     DateTime? @map("last_used_at")
  totalImported  Int       @default(0) @map("total_imported")
  avgSuccessRate Decimal?  @map("avg_success_rate") @db.Decimal(5, 2)

  status        ProfileStatus @default(ACTIVE)
  isDefault     Boolean       @default(false) @map("is_default")
  createdById   String        @map("created_by_id")
  createdByName String        @map("created_by_name")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")

  importJobs ImportJob[]
```

Models: 12

### apps-backend/api/prisma/working/v1/inventory.prisma

```prisma
// WorkingDB — inventory domain
// Generated by scripts/split-schemas.py
// Models: Product, ProductPrice, CustomerPriceGroup, CustomerGroupMapping, ProductTaxDetail, ProductUnitConversion, ProductRelation, ProductFilter, InventoryItem, StockLocation, StockTransaction, StockSummary, StockAdjustment, SerialMaster, BOMFormula, BOMFormulaItem, BOMProduction, ScrapRecord, InventoryLabel, UnitMaster, UnitConversion, PurchaseRFQ, PurchaseRFQItem, PurchaseQuotation, PurchaseQuotationItem, QuotationComparison, PurchaseOrder, PurchaseOrderItem, GoodsReceipt, GoodsReceiptItem, PurchaseMaster

model Product {
  id               String  @id @default(uuid())
  tenantId         String  @default("") @map("tenant_id")
  name             String
  code             String
  slug             String
  shortDescription String? @map("short_description")
  description      String?

  // Hierarchy
  parentId String?   @map("parent_id")
  parent   Product?  @relation("ProductHierarchy", fields: [parentId], references: [id])
  children Product[] @relation("ProductHierarchy")
  isMaster Boolean   @default(false) @map("is_master")

  // Media
  image       String?
  images      Json?
  brochureUrl String? @map("brochure_url")
  videoUrl    String? @map("video_url")

  // Pricing (base)
  mrp           Decimal? @db.Decimal(12, 2)
  salePrice     Decimal? @map("sale_price") @db.Decimal(12, 2)
  purchasePrice Decimal? @map("purchase_price") @db.Decimal(12, 2)
  costPrice     Decimal? @map("cost_price") @db.Decimal(12, 2)

  // Tax (Indian GST)
  taxType      TaxType  @default(GST) @map("tax_type")
  hsnCode      String?  @map("hsn_code")
  gstRate      Decimal? @map("gst_rate") @db.Decimal(5, 2)
  cessRate     Decimal? @map("cess_rate") @db.Decimal(5, 2)
  taxInclusive Boolean  @default(false) @map("tax_inclusive")

  // Unit & Packing
  primaryUnit      UnitType  @default(PIECE) @map("primary_unit")
```

Models: 31

### apps-backend/api/prisma/working/v1/notifications.prisma

```prisma
// WorkingDB — notifications domain
// Generated by scripts/split-schemas.py
// Models: Reminder, Notification, NotificationPreference, NotificationTemplate, PushSubscription, NotificationConfig, EscalationRule

model Reminder {
  id          String          @id @default(uuid())
  tenantId    String          @default("") @map("tenant_id")
  entityType  String          @map("entity_type")
  entityId    String          @map("entity_id")
  channel     ReminderChannel @default(IN_APP)
  scheduledAt DateTime        @map("scheduled_at")
  sentAt      DateTime?       @map("sent_at")
  isSent      Boolean         @default(false) @map("is_sent")
  title       String
  message     String?         @db.Text
  description String?         @db.Text

  // ─── Enhanced fields ───
  status           ReminderStatus @default(PENDING)
  type             ReminderType   @default(PERSONAL)
  recurrenceConfig Json?          @map("recurrence_config")
  snoozedUntil     DateTime?      @map("snoozed_until")
  snoozeCount      Int            @default(0) @map("snooze_count")
  maxSnooze        Int            @default(3) @map("max_snooze")
  triggeredAt      DateTime?      @map("triggered_at")
  acknowledgedAt   DateTime?      @map("acknowledged_at")
  missedAt         DateTime?      @map("missed_at")
  taskId           String?        @map("task_id")
  task             Task?          @relation("TaskReminders", fields: [taskId], references: [id])

  // ─── NOTIFICATION PREFERENCES ───
  notifyVia Json @default("[\"PUSH\",\"IN_APP\"]") @map("notify_via")

  // ─── End enhanced ───
  recipientId String   @map("recipient_id")
  // [cross-db removed] recipient   User     @relation("ReminderRecipient", fields: [recipientId], references: [id])
  createdById String   @map("created_by_id")
  // [cross-db removed] createdBy   User     @relation("ReminderCreatedBy", fields: [createdById], references: [id])
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")
```

Models: 7

### apps-backend/api/prisma/working/v1/payments.prisma

```prisma
// WorkingDB — payments domain
// Generated by scripts/split-schemas.py
// Models: Payment, PaymentReceipt, Refund, PaymentReminder, PaymentRecord

model Payment {
  id        String  @id @default(uuid())
  tenantId  String  @default("") @map("tenant_id")
  paymentNo String  @map("payment_no")
  invoiceId String  @map("invoice_id")
  invoice   Invoice @relation(fields: [invoiceId], references: [id])

  // ─── PAYMENT INFO ───
  amount   Decimal        @db.Decimal(14, 2)
  currency String         @default("INR")
  status   PaymentStatus  @default(PENDING)
  method   PaymentMethod
  gateway  PaymentGateway @default(MANUAL)

  // ─── GATEWAY DETAILS ───
  gatewayOrderId   String? @map("gateway_order_id")
  gatewayPaymentId String? @map("gateway_payment_id")
  gatewaySignature String? @map("gateway_signature")
  gatewayResponse  Json?   @map("gateway_response")

  // ─── OFFLINE DETAILS ───
  chequeNumber     String?   @map("cheque_number")
  chequeDate       DateTime? @map("cheque_date")
  chequeBankName   String?   @map("cheque_bank_name")
  transactionRef   String?   @map("transaction_ref")
  upiTransactionId String?   @map("upi_transaction_id")

  // ─── META ───
  paidAt        DateTime? @map("paid_at")
  notes         String?
  failureReason String?   @map("failure_reason")

  // ─── AUDIT ───
  recordedById String   @map("recorded_by_id")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")
```

Models: 5

### apps-backend/api/prisma/working/v1/reports.prisma

```prisma
// WorkingDB — reports domain
// Generated by scripts/split-schemas.py
// Models: ReportExportLog, ReportDefinition, ReportBookmark, ScheduledReport, ReportTemplate

model ReportExportLog {
  id                String       @id @default(uuid())
  tenantId          String       @default("") @map("tenant_id")
  reportType        ReportType   @map("report_type")
  format            ReportFormat
  filters           Json?
  recordCount       Int          @map("record_count")
  fileUrl           String?      @map("file_url")
  fileSize          Int?         @map("file_size")
  status            String       @default("COMPLETED")
  errorMessage      String?      @map("error_message")
  generatedAt       DateTime     @map("generated_at")
  duration          Int?
  exportedById      String       @map("exported_by_id")
  exportedByName    String       @map("exported_by_name")
  reportCode        String?      @map("report_code")
  reportName        String?      @map("report_name")
  exportSource      String?      @default("MANUAL") @map("export_source")
  scheduledReportId String?      @map("scheduled_report_id")
  generationTimeMs  Int?         @map("generation_time_ms")
  createdAt         DateTime     @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([exportedById, createdAt])
  @@index([reportCode, createdAt])
  @@index([tenantId])
  @@map("gv_rpt_export_logs")
}

model ReportDefinition {
```

Models: 5

### apps-backend/api/prisma/working/v1/sales.prisma

```prisma
// WorkingDB — sales domain
// Generated by scripts/split-schemas.py
// Models: Quotation, QuotationLineItem, QuotationSendLog, QuotationNegotiationLog, QuotationActivity, QuotationTemplate, SalesTarget, ServiceRate, SaleOrder, SaleOrderItem, DeliveryChallan, DeliveryChallanItem, SaleReturn, SaleReturnItem, SaleMaster, PriceList, PriceListItem

model Quotation {
  id          String          @id @default(uuid())
  tenantId    String          @default("") @map("tenant_id")
  quotationNo String          @map("quotation_no")
  status      QuotationStatus @default(DRAFT)
  version     Int             @default(1)

  // ─── BUSINESS INFO ───
  title     String?
  summary   String?
  coverNote String? @map("cover_note")

  // ─── PRICING ───
  subtotal       Decimal @default(0) @db.Decimal(14, 2)
  discountType   String? @map("discount_type")
  discountValue  Decimal @default(0) @map("discount_value") @db.Decimal(14, 2)
  discountAmount Decimal @default(0) @map("discount_amount") @db.Decimal(14, 2)
  taxableAmount  Decimal @default(0) @map("taxable_amount") @db.Decimal(14, 2)
  cgstAmount     Decimal @default(0) @map("cgst_amount") @db.Decimal(14, 2)
  sgstAmount     Decimal @default(0) @map("sgst_amount") @db.Decimal(14, 2)
  igstAmount     Decimal @default(0) @map("igst_amount") @db.Decimal(14, 2)
  cessAmount     Decimal @default(0) @map("cess_amount") @db.Decimal(14, 2)
  totalTax       Decimal @default(0) @map("total_tax") @db.Decimal(14, 2)
  roundOff       Decimal @default(0) @map("round_off") @db.Decimal(14, 2)
  totalAmount    Decimal @default(0) @map("total_amount") @db.Decimal(14, 2)

  // ─── PRICE FLEXIBILITY ───
  priceType        QuotationPriceType @default(FIXED) @map("price_type")
  minAmount        Decimal?           @map("min_amount") @db.Decimal(14, 2)
  maxAmount        Decimal?           @map("max_amount") @db.Decimal(14, 2)
  plusMinusPercent Decimal?           @map("plus_minus_percent") @db.Decimal(5, 2)

  // ─── VALIDITY & TERMS ───
  validFrom       DateTime? @map("valid_from")
  validUntil      DateTime? @map("valid_until")
  paymentTerms    String?   @map("payment_terms")
```

Models: 17

### apps-backend/api/prisma/working/v1/tax.prisma

```prisma
// WorkingDB — tax domain
// Generated by scripts/split-schemas.py
// Models: GstVerificationLog, GSTReturn, TDSRecord

model GstVerificationLog {
  id                 String   @id @default(uuid())
  tenantId           String   @map("tenant_id")
  userId             String   @map("user_id")
  gstNumber          String   @map("gst_number")
  verificationMethod String   @map("verification_method")
  apiResponse        Json?    @map("api_response")
  isValid            Boolean  @map("is_valid")
  businessName       String?  @map("business_name")
  businessAddress    String?  @map("business_address")
  gstStatus          String?  @map("gst_status")
  approvedBy         String?  @map("approved_by")
  approvalNotes      String?  @map("approval_notes")
  createdAt          DateTime @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([tenantId, userId])
  @@index([gstNumber])
  @@map("gv_tax_gst_verification_logs")
}

model GSTReturn {
  id                String    @id @default(uuid())
  tenantId          String    @map("tenant_id")
  returnType        String    @map("return_type")
  period            String
  financialYear     String    @map("financial_year")
  b2bInvoices       Json?     @map("b2b_invoices")
  b2cLarge          Json?     @map("b2c_large")
  b2cSmall          Json?     @map("b2c_small")
```

Models: 3

### apps-backend/api/prisma/working/v1/workflow.prisma

```prisma
// WorkingDB — workflow domain
// Generated by scripts/split-schemas.py
// Models: ApprovalRequest, ApprovalRule, Workflow, WorkflowState, WorkflowTransition, WorkflowInstance, WorkflowHistory, WorkflowApproval, WorkflowActionLog, WorkflowSlaEscalation

model ApprovalRequest {
  id          String    @id @default(uuid())
  tenantId    String    @default("") @map("tenant_id")
  entityType  String    @map("entity_type")
  entityId    String?   @map("entity_id")
  action      String
  payload     Json
  makerId     String    @map("maker_id")
  // [cross-db removed] maker       User      @relation("ApprovalMaker", fields: [makerId], references: [id])
  checkerId   String?   @map("checker_id")
  // [cross-db removed] checker     User?     @relation("ApprovalChecker", fields: [checkerId], references: [id])
  checkerRole String    @map("checker_role")
  status      String    @default("PENDING")
  makerNote   String?   @map("maker_note")
  checkerNote String?   @map("checker_note")
  expiresAt   DateTime  @map("expires_at")
  decidedAt   DateTime? @map("decided_at")
  createdAt   DateTime  @default(now()) @map("created_at")

  isDeleted     Boolean   @default(false) @map("is_deleted")
  deletedAt     DateTime? @map("deleted_at")
  deletedById   String?   @map("deleted_by_id")
  updatedById   String?   @map("updated_by_id")
  updatedByName String?   @map("updated_by_name")

  @@index([isDeleted])
  @@index([status])
  @@index([makerId])
  @@index([checkerRole, status])
  @@index([tenantId])
  @@map("gv_wf_approval_requests")
}

model ApprovalRule {
  id              String   @id @default(uuid())
  tenantId        String   @default("") @map("tenant_id")
```

Models: 10

## Prisma Client Output Paths

## Database Summary Table

| Schema File | Models | Client Output |
|---|---|---|
| _base.prisma | 0
0 | default |
| accounts.prisma | 16 | default |
| audit.prisma | 7 | default |
| communication.prisma | 21 | default |
| config.prisma | 50 | default |
| crm.prisma | 44 | default |
| documents.prisma | 12 | default |
| inventory.prisma | 31 | default |
| notifications.prisma | 7 | default |
| payments.prisma | 5 | default |
| reports.prisma | 5 | default |
| sales.prisma | 17 | default |
| tax.prisma | 3 | default |
| workflow.prisma | 10 | default |
| _base.prisma | 0
0 | default |
| indian-reference.prisma | 3 | default |
| reference.prisma | 5 | default |
| system-reference.prisma | 4 | default |
| _base.prisma | 0
0 | default |
| audit.prisma | 7 | default |
| config.prisma | 12 | default |
| licensing.prisma | 6 | default |
| users.prisma | 18 | default |
| _base.prisma | 0
0 | default |
| marketplace.prisma | 13 | default |
| _base.prisma | 0
0 | default |
| devops.prisma | 25 | default |
| testing.prisma | 4 | default |
| _base.prisma | 0
0 | default |
| audit.prisma | 4 | default |
| marketplace.prisma | 13 | default |
| modules.prisma | 19 | default |
| subscriptions.prisma | 14 | default |
| testing.prisma | 13 | default |
| vendors.prisma | 1 | default |
| _base.prisma | 0
0 | default |
| accounts.prisma | 16 | default |
| audit.prisma | 7 | default |
| communication.prisma | 21 | default |
| config.prisma | 50 | default |
| crm.prisma | 44 | default |
| documents.prisma | 12 | default |
| inventory.prisma | 31 | default |
| notifications.prisma | 7 | default |
| payments.prisma | 5 | default |
| reports.prisma | 5 | default |
| sales.prisma | 17 | default |
| tax.prisma | 3 | default |
| workflow.prisma | 10 | default |
