# Sprint G — Prisma CI Lint — Completion Report

**Date:** 2026-04-16
**Sprint:** Sprint G — CI Lint for Prisma File Organization
**Verdict:** ✅ COMPLETE

---

## Summary

Sprint G locks the prismaSchemaFolder layout from Sprint F with a 7-rule lint script
that runs in CI on every PR. All stale `prisma/schemas/` references across 4 CI workflows,
2 shell scripts, package.json, and 8 TypeScript comment lines have been removed.
The legacy monolithic `prisma/schema.prisma` (13578 lines) has been deleted.

---

## Files created

| File | Description |
|------|-------------|
| `Application/backend/scripts/lint/prisma-structure-lint.sh` | 7-rule Prisma structure lint script |
| `docs/discovery/2026-04-16_sprint_g_ci_lint_scan.md` | Discovery scan report |

---

## Files updated

| File | Change |
|------|--------|
| `Application/backend/package.json` | `build` script uses folder-based generate; added `lint:prisma` script; fixed `prisma:generate` |
| `.github/workflows/pr-check.yml` | Fixed 2× generate steps; removed `--soft-fail` from db-audit; added `prisma-lint` job |
| `.github/workflows/release-gate.yml` | Fixed 2× generate steps |
| `.github/workflows/release.yml` | Fixed 1× generate step |
| `.github/workflows/nightly-tests.yml` | Fixed 1× generate step |
| `scripts/architecture-guard.sh` | Updated 2× `prisma/schemas/*.prisma` → `prisma/*/v1/*.prisma` |
| `scripts/weekly-health-check.sh` | Updated 3× `prisma/schemas/` references |
| 8× `src/modules/marketplace/*/infrastructure/mkt-prisma.service.ts` | Updated `@ts-ignore` comment |

---

## Files deleted

| File | Reason |
|------|--------|
| `Application/backend/prisma/schema.prisma` | 13578-line legacy monolith, pre-Sprint-F artifact, no longer needed |

---

## Lint rules

| Rule | What it checks | Severity |
|------|---------------|----------|
| 1 | `prisma/schemas/` directory must NOT exist | error |
| 2 | Every approved DB folder (`identity`, `platform`, `working`, `marketplace`, `platform-console`, `global`) has `_base.prisma` | error |
| 3 | No `.prisma` files outside approved `prisma/*/v1/` folders | error |
| 4 | `generator`/`datasource` blocks only in `_base.prisma` | error |
| 5 | No `prisma/schemas/` references in `*.ts`, `*.yml`, `*.sh`, `*.json` | error |
| 6 | `prismaSchemaFolder` preview feature declared in all `_base.prisma` files | error |
| 7 | `@@map` prefix matches expected folder (IdentityDB=gv_usr_/gv_cfg_, etc.) | warning |

---

## Verification

| Check | Result |
|-------|--------|
| `bash scripts/lint/prisma-structure-lint.sh` | ✅ 0 errors, 0 warnings |
| Deliberate fail test (stray file → Rule 1 + Rule 3 fired → removed) | ✅ Confirmed |
| `npm run typecheck` (tsc --noEmit) | ✅ 0 errors |
| DB auditor (naming / crossDb / fkOrphan) | ✅ 0 / 0 / 0 |
| `prisma/schemas/` references remaining in codebase | ✅ 0 |

---

## CI gate added

`pr-check.yml` now runs `prisma-lint` job on every PR targeting `main` or `develop`.
No Node.js or database setup required — pure bash/grep, completes in <10s.

**Recommended:** Add "Prisma Structure Lint" to required status checks in GitHub branch
protection settings for `main` and `develop` (Settings → Branches → Required status checks).

---

## Architecture v2 Sprint completion status

| Sprint | Description | Status |
|--------|-------------|--------|
| A | DB drift cleanup | ✅ |
| B | GlobalReferenceDB | ✅ |
| C | DemoDB wiring | ⚠️ architectural gap (future) |
| D | HAS_DATA_CRITICAL | ✅ (folded into A) |
| E | test_plans collision | ✅ |
| F | Prisma schema folder split | ✅ |
| **G** | **CI lint for Prisma file organization** | **✅ THIS** |

**Architecture v2 pre-production cleanup sprint: COMPLETE**
Foundation is locked. Next phase: vertical rollout (restaurant, tourism, retail).

---

*Generated by Claude Code session 2026-04-16*
