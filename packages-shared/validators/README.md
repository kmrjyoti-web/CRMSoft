# @crmsoft/validators

**Status:** PLACEHOLDER

## Purpose

Cross-cutting validators used by both `apps-backend/` and `apps-frontend/` packages.

## V5 Integration

After Phase 5, importable via tsconfig path alias `@crmsoft/validators/*`. Until then, this folder is empty scaffolding.
