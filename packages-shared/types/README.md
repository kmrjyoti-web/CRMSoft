# @crmsoft/types

**Status:** PLACEHOLDER

## Purpose

Cross-cutting types used by both `apps-backend/` and `apps-frontend/` packages.

## V5 Integration

After Phase 5, importable via tsconfig path alias `@crmsoft/types/*`. Until then, this folder is empty scaffolding.
