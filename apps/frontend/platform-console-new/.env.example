# API (server-side SSR fetches)
INTERNAL_API_URL=http://localhost:3001

# API (client-side fetches)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Port (set in npm scripts — no need to configure here)
# PORT=3012
