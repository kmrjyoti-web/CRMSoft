# Sprint 3 Execution Log — 2026-04-26

## Built

### Backend (additions to Sprint 2 module)
- `POST /api/v1/onboarding/complete-profile` — saves `verticalCode`, `registrationFields`, marks `onboardingComplete = true`, returns `{ success, redirectTo }`
- `CompleteProfileDto` — `verticalCode: string`, `profileFields?: Record<string, unknown>`

### Frontend (`features/user-onboarding/`)
- `user-onboarding.service.ts` — typed API calls, default `api` export from api-client
- `stores/user-onboarding.store.ts` — Zustand store with `fetchStatus / setLocale / sendOtp / verifyOtp / skipMobile / setUserType / completeProfile`
- `stages/StageLanguage.tsx` — `LanguageSelector` + locale save, next-intl keys
- `stages/StageEmailOtp.tsx` — OTP send on mount, 60s cooldown, `OTPInput`, verify + error states
- `stages/StageMobileOtp.tsx` — OTP verify or skip button
- `stages/StageUserType.tsx` — 2×2 card grid (B2B/B2C/IND_SP/IND_EE)
- `stages/StageVerticalProfile.tsx` — 3×2 vertical grid + optional company name → `completeProfile`
- `OnboardingDialog.tsx` — `Modal` wrapper (`closeOnOverlay={false}`, `closeOnEscape={false}`), `OnboardingStepper` in header, stage router

### CRM Portal Integration
- `(main)/layout.tsx` — `<OnboardingDialog />` injected after `<AiChatWidget />`
- Renders null when: not authenticated, still loading, `status.complete === true`
- Guard: reads `useAuthStore(s => s.token)` before fetching onboarding status

### Demo Reset Script
- `prisma/seeds/identity/reset-onboarding.ts` — resets all 5 onboarding fields + expires pending OTPs
- Usage: `npx ts-node --transpile-only prisma/seeds/identity/reset-onboarding.ts kmrjyoti@gmail.com`

## Architecture Notes
- Kept separate from existing `features/onboarding/` (tenant/company setup wizard) — no conflicts
- All UI text via `useTranslations()` — 3-language support from day 1
- All colors via `var(--color-*)` CSS vars — matches existing CRM theme system
- `Modal` reuses existing AICModal wrapper (no new component created)
- `OTPInput` reuses existing AICOTPInput wrapper
- `Button` reuses existing AICButton wrapper
- Stage 5 simplified for demo: saves `verticalCode` + optional `companyName` as `registrationFields`

## TypeScript Status
- Backend: ✅ 0 errors
- Frontend: ✅ 0 new errors from Sprint 3 files

## Apr 28 Demo Status: READY (pending smoke test with running servers)

## Smoke Test Checklist
- [ ] Stage 1 (Language): hī/mr/en select → Continue → stage advances
- [ ] Stage 2 (Email OTP): OTP auto-sent on mount → console code → verify → stage advances
- [ ] Stage 3 (Mobile skip): Skip → stage advances to user_type
- [ ] Stage 4 (User Type): B2B/B2C/IND_SP/IND_EE select → Continue
- [ ] Stage 5 (Vertical): pick TRAVEL → optional company name → Complete
- [ ] Dialog closes after Stage 5 complete
- [ ] Page refresh → dialog does NOT reappear (onboardingComplete = true)
- [ ] Reset script → onboardingComplete = false → dialog reappears on next login

## Demo Reset Command
```bash
cd apps-backend/api
npx ts-node --transpile-only prisma/seeds/identity/reset-onboarding.ts kmrjyoti@gmail.com
```
