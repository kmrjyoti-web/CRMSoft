# Sprint 4 Execution Log — 2026-04-26

## Summary: Remove /self-care, redirect login → /dashboard

The self-care panel was a Day 2 intermediate landing page (LinkedIn-style workspace picker). It is superseded by:
- `OnboardingDialog` (already in `(main)/layout.tsx`) handles the 5-stage onboarding wall
- `/dashboard` is the correct authenticated home

## Changes

### Frontend — Redirect targets (7 replacements, `/self-care` → `/dashboard`)
- `src/middleware.ts` — authenticated-on-public-route redirect
- `src/features/auth/components/LoginForm.tsx` — post-login push
- `src/components/brand-login/brands/travvellis/TravvellisLogin.tsx` — post-login push
- `src/components/company/CompanyHeader.tsx` (×2) — Home button + "All workspaces" dropdown item (renamed "Dashboard")
- `src/app/company/[companyId]/layout.tsx` — missing-company guard fallback

### Frontend — Archived (moved to `src/.archive/2026-04-26/`)
- `src/app/self-care/page.tsx` — workspace picker page
- `src/app/self-care/layout.tsx` — standalone layout wrapper
- `src/components/self-care/ProfileSummaryCard.tsx` — only used by self-care page
- `src/components/self-care/CompanyCard.tsx` — only used by self-care page

### Backend — Cleanup
- `onboarding.service.ts` — `redirectTo: '/self-care'` → `/dashboard`
- `auth.service.ts` — `redirectPath` always `/dashboard`; `purpose` token field updated

## Architecture After This Sprint

```
Login → /dashboard → (main)/layout.tsx
                      ├── OnboardingDialog (renders if onboardingComplete=false)
                      └── Dashboard content (renders behind dialog)
```

## Status: COMPLETE
