# Shared Prisma Schemas

> ⚠️ **CURRENTLY STALE — DO NOT USE**
>
> These files are reference snapshots and have NOT been kept in sync with the
> active schemas. As of 2026-04-13 they are 9 days behind and missing changes
> from Sprint A, Sprint A.5, and Week 1 (incl. `gv_cfg_vertical` registry).
>
> `platform-console.prisma` is also missing from this folder entirely.
>
> **Canonical source:** `Application/backend/prisma/schemas/`
>
> Auto-sync will be set up during the SDK publishing sprint
> (`@crmsoft/prisma-schemas` package). Until then, do not import from here.

## (when synced)

- identity.prisma — IdentityDB (42 models)
- platform.prisma — PlatformDB (37+ models, incl. gv_cfg_vertical)
- platform-console.prisma — PlatformConsoleDB (30 models) — MISSING from Shared today
- working.prisma — WorkingDB
- marketplace.prisma — MarketplaceDB
- WhiteLabel schema lives in `WhiteLabel/wl-api/prisma/`
