{
  "name": "@crmsoft/prisma",
  "version": "1.0.0",
  "description": "CRMSoft Prisma SDK — base PrismaService, soft-delete middleware, multi-schema utilities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kmrjyoti-web/CrmProject.git",
    "directory": "Shared/backend/prisma"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "@nestjs/common": "^10.4.22",
    "@nestjs/core": "^10.4.22",
    "@prisma/client": "^5.22.0",
    "reflect-metadata": "^0.2.2",
    "rimraf": "^5.0.10",
    "typescript": "^5.9.3"
  }
}
