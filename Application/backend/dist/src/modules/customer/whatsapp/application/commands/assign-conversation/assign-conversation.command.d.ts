export declare class AssignConversationCommand {
    readonly conversationId: string;
    readonly assignToUserId: string;
    readonly userId: string;
    constructor(conversationId: string, assignToUserId: string, userId: string);
}
