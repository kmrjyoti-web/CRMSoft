export declare class SearchConversationsQuery {
    readonly wabaId: string;
    readonly query: string;
    readonly page: number;
    readonly limit: number;
    constructor(wabaId: string, query: string, page: number, limit: number);
}
