export declare class GetConversationMessagesQuery {
    readonly conversationId: string;
    readonly page: number;
    readonly limit: number;
    constructor(conversationId: string, page: number, limit: number);
}
