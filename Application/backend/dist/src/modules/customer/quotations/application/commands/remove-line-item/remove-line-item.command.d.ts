export declare class RemoveLineItemCommand {
    readonly quotationId: string;
    readonly itemId: string;
    readonly userId: string;
    readonly userName: string;
    constructor(quotationId: string, itemId: string, userId: string, userName: string);
}
