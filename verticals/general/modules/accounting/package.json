{
  "name": "@crmsoft/gv-accounting",
  "version": "0.1.0",
  "description": "General Vertical — Accounting modules (accounts, payment, wallet, quotations, sales, procurement, approvals, documents)",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
