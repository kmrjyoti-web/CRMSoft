name: CI — crm-admin

on:
  pull_request:
    branches: [develop, main]
    paths:
      - 'Customer/frontend/crm-admin/**'
      - '.github/workflows/ci-crm-admin.yml'
  push:
    branches: [develop]
    paths:
      - 'Customer/frontend/crm-admin/**'

concurrency:
  group: ci-crm-admin-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'
  PNPM_VERSION: '9'
  NODE_OPTIONS: '--max-old-space-size=4096'
  APP_DIR: 'Customer/frontend/crm-admin'
  COREUI_DIR: 'Customer/frontend/crm-admin/lib/coreui'

jobs:
  # ─────────────────────────────────────────────
  typecheck:
    name: Type check (tsc)
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: 'Customer/frontend/crm-admin/pnpm-lock.yaml'
      - name: Install crm-admin
        working-directory: ${{ env.APP_DIR }}
        run: pnpm install --frozen-lockfile
      - name: Install + build coreui (turbo workspaces)
        working-directory: ${{ env.COREUI_DIR }}
        run: |
          pnpm install --frozen-lockfile
          pnpm build
      - name: typecheck
        working-directory: ${{ env.APP_DIR }}
        # Uses the project's custom script which filters lib/coreui errors.
        run: pnpm typecheck

  # ─────────────────────────────────────────────
  # ⚠️ Lint is non-blocking in v1 — tighten in follow-up sprint.
  # ─────────────────────────────────────────────
  lint:
    name: Lint — non-blocking v1
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: 'Customer/frontend/crm-admin/pnpm-lock.yaml'
      - name: Install crm-admin
        working-directory: ${{ env.APP_DIR }}
        run: pnpm install --frozen-lockfile
      - name: next lint
        working-directory: ${{ env.APP_DIR }}
        run: pnpm lint
        continue-on-error: true

  # ─────────────────────────────────────────────
  build:
    name: Next.js build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: 'Customer/frontend/crm-admin/pnpm-lock.yaml'
      - name: Install crm-admin
        working-directory: ${{ env.APP_DIR }}
        run: pnpm install --frozen-lockfile
      - name: Install + build coreui
        working-directory: ${{ env.COREUI_DIR }}
        run: |
          pnpm install --frozen-lockfile
          pnpm build
      - name: next build
        working-directory: ${{ env.APP_DIR }}
        env:
          NEXT_PUBLIC_API_URL: 'http://localhost:3001'
          NEXT_TELEMETRY_DISABLED: '1'
        run: pnpm build
