# Shared Frontend Hooks
- useEntityPanel — CRUD side panels (all portals)
- useContentPanel — info/help panels
- useSmartSearch — field-key search (SmartSearch)
- useTableConfig — saved column configs
- useDataMasking — RBAC field masking
